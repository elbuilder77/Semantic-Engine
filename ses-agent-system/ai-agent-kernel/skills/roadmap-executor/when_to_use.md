# When to use

- When a new sprint or development cycle begins.
- When the agent is handed a list of features or a `TODO.md` file.
- When an objective spans multiple files and requires a phased approach.
- To prevent the agent from attempting to solve an entire project in a single prompt.
