# Purpose

To systematically test and validate all external and internal API boundaries within the project, ensuring contracts are upheld, responses are predictable, and edge cases are handled gracefully. This skill provides the framework for orchestrating test execution independently of the specific testing framework used by the project.
