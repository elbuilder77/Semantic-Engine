# When to use

Use this skill when the task involves:
- `RateLimitMiddleware`
- Redis-up / Redis-down behavior
- invalid API key handling
- auth hardening
- removing or isolating fail-open behavior
- `.env` strategy for sensitive config
- response sanitization
- request rejection behavior under unsafe conditions

Use it whenever security behavior changes or when Phase 1 is still open.
