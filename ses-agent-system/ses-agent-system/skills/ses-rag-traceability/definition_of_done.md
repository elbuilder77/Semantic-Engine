# Definition of done

This skill is done for a given slice only when:
- source evidence can be followed from retrieval into the output contract
- answer/export behavior is consistent enough to audit
- empty or no-evidence cases are explicitly handled
- tests prove the intended behavior

This skill is not done if citations exist visually but cannot be trusted structurally.
