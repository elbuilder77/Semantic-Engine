# Definition of done

This skill is done for a given endpoint slice only when:
- the endpoint behavior is understood
- integration coverage exists or is updated
- the endpoint passes its targeted validation
- Docker/local behavior is not contradictory for that path
- residual dependency risk is stated

This skill is not done if only unit tests pass but the real stack path remains unproven.
